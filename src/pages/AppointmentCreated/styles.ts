import styled from 'styled-components/native';

const Welcome = styled.Text`
  font-size: 22px;
  color: #f4ede8;
  font-family: 'RobotoSlab-Medium';
  margin-bottom: 24px;
`;

const Username = styled.Text`
  font-size: 14px;
  color: #f4ede8;
  font-family: 'RobotoSlab-Medium';
`;

const Email = styled.Text`
  font-size: 10px;
  color: #f4ede8;
  font-family: 'RobotoSlab-Medium';
  margin-bottom: 24px;
`;

export { Welcome, Username, Email };
